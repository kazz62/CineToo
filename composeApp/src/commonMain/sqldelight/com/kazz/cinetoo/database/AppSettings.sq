CREATE TABLE AppSettings (
    key TEXT NOT NULL PRIMARY KEY,
    settingValue TEXT NOT NULL
);

selectByKey:
SELECT * FROM AppSettings WHERE key = ?;

insert:
INSERT OR REPLACE INTO AppSettings(key, settingValue)
VALUES (?, ?);

deleteAll:
DELETE FROM AppSettings;
