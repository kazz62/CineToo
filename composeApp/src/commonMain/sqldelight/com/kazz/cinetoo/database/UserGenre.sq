CREATE TABLE UserGenre (
    id INTEGER NOT NULL PRIMARY KEY,
    name TEXT NOT NULL,
    emoji TEXT NOT NULL
);

selectAll:
SELECT * FROM UserGenre;

insert:
INSERT OR REPLACE INTO UserGenre(id, name, emoji)
VALUES (?, ?, ?);

delete:
DELETE FROM UserGenre WHERE id = ?;

deleteAll:
DELETE FROM UserGenre;
